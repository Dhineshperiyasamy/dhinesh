#include<stdio.h>
int main()
{
int a,b;
printf("\nenter two numbers:");
scanf("%d%d",&x,&z);
printf("\nnumber before swapping are:%d%d",x,z);
a=a^c;
c=a^c;
a=a^c;
printf("\nnumbers after swapping:%d%d",x,z);
return 0;
}
