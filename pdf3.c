#include<stdio.h>
void main()
{
int a,w;
int arr[10];
printf("enter the total number ");
scanf("%d",&w);
for(i=0;i<w;++i)
{
printf("enter the number %d";i+1)
scanf("%d",&arr[i]);
for(i=1;i<w;++i)
{
if(arr[0]<arr[a])
arr[0]=arr[a];
}
printf("maximum element=%d",arr[a]);
}
}
